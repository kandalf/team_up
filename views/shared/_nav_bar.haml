%nav.navbar.navbar-static-top
  .navbar-inner
    .container
      %a.brand{ :href => "#" }
        TeamUp
      %ul.nav
        %li{ :class => (req.path == '/dashboard') ? 'active' : '' }
          = link_to '/dashboard', 'Dashboard'

      .pull-right
        .user
          = current_user ? current_user.github_user : ''
